# Copy to .env (gitignored) and fill in the value.
#
# GITHUB_TOKEN: fine-grained personal access token used by
# bin/pull-modules.php to clone YayCommerce/cross-sell-manager.
# Required scope: Contents (Read-only) on that single repo.
# Generate at: https://github.com/settings/personal-access-tokens

GITHUB_TOKEN=
